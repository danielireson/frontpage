# frontpage.today

```
# create config file
cp config/exmaple.json config/prod.json

# invoke locally
./scripts/build

# create stack
./scripts/create-stack

# update stack
./scripts/update-stack

```
