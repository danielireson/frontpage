<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Today's frontpage</title>
  </head>
  <style>
    html {
      box-sizing: border-box;
      background: rgb(249, 249, 249);
      color: rgb(47, 54, 64);
      font-size: 18px;
      font-family: Georgia, "Times New Roman", Times, serif;
    }

    body {
      display: flex;
      min-height: 100vh;
      align-items: center;
      justify-content: center;
    }

    *,
    *:before,
    *:after {
      box-sizing: inherit;
    }

    body,
    h1,
    h2,
    h3,
    h4,
    h5,
    h6,
    p,
    ol,
    ul {
      margin: 0;
      padding: 0;
      font-weight: normal;
    }

    ol,
    ul {
      list-style: none;
    }

    a {
      color: rgb(0, 81, 255);
      text-decoration: none;
    }

    a:hover {
      text-decoration: underline;
    }

    header {
      display: flex;
      flex-direction: column;
      justify-content: space-between;
      padding: 1rem;
    }

    header .header-title {
      margin: 1rem 0;
      font-size: 2rem;
      line-height: 1;
    }

    .header-about {
      display: none;
    }

    @media (min-width: 769px) {
      header {
        align-items: center;
      }

      header .header-about {
        display: block;
        position: absolute;
        top: 1rem;
        right: 1rem;
      }
    }

    main {
      padding: 1rem;
      margin: auto auto 2rem auto;
    }

    main .main-news-item {
      margin-bottom: 1rem;
    }

    main .main-news-item:last-child {
      margin-bottom: 0;
    }

    main .main-news-link {
      word-break: break-word;
    }

    main .main-news-link:visited {
      color: rgb(131, 165, 236);
    }

    @media (min-width: 769px) {
      main {
        max-width: 1024px;
      }

      main .main-news-item {
        display: grid;
        grid-template-rows: 1fr auto;
        break-inside: avoid;
        margin-bottom: 2rem;
        text-align: left;
      }

      main .main-news-items {
        column-count: 4;
        column-gap: 2rem;
      }
    }
  </style>
  <body>
    <div class="container">
      <header>
        <h1 class="header-title">Today's Frontpage</h1>
        <span class="header-edition">{{name}}</span>
        <a
          href="https://github.com/danielireson/frontpage.today"
          target="_blank"
          rel="nofollow noopener"
          class="header-about"
        >
          About this site
        </a>
      </header>
      <main>
        <ul class="main-news-items">
          {{#each items as |item|}}
          <li class="main-news-item">
            <a
              href="{{item.link}}"
              class="main-news-link"
              target="_blank"
              rel="noopener noreferrer"
            >
              {{item.title}}
            </a>
          </li>
          {{/each}}
        </ul>
      </main>
    </div>
  </body>
</html>
